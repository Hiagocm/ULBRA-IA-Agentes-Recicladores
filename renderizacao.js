var c = document.getElementById("myCanvas"),
    ctx = c.getContext("2d");
/*
(function renderLoop() {
    'use strict';
    window.requestAnimationFrame(renderLoop);
    update();
    draw();
}());*/